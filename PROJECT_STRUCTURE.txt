react_conversion/
│
├── 📁 src/
│   ├── 📁 components/
│   │   ├── 📁 Layout/
│   │   │   ├── Layout.jsx          # Main layout wrapper
│   │   │   ├── Sidebar.jsx         # Left navigation sidebar
│   │   │   └── Header.jsx          # Top header with logout
│   │   ├── ProfileModal.jsx        # Employee profile popup
│   │   ├── ProtectedRoute.jsx      # Route authorization
│   │   └── ThemeToggle.jsx         # Dark/light theme button
│   │
│   ├── 📁 pages/
│   │   ├── Login.jsx               # Login screen
│   │   ├── Dashboard.jsx           # Analytics dashboard
│   │   ├── EmployeeList.jsx        # Employee directory
│   │   ├── AddEmployee.jsx         # Add employee forms
│   │   └── ManageUsers.jsx         # User management
│   │
│   ├── 📁 context/
│   │   ├── AuthContext.jsx         # Authentication state
│   │   └── ThemeContext.jsx        # Theme state
│   │
│   ├── 📁 services/
│   │   └── api.js                  # API calls (needs backend)
│   │
│   ├── App.jsx                     # Main app + routing
│   ├── main.jsx                    # Entry point
│   └── index.css                   # Global styles
│
├── 📁 public/                      # Static files
│
├── 📄 index.html                   # HTML template
├── 📄 package.json                 # Dependencies
├── 📄 vite.config.js              # Build config
├── 📄 tailwind.config.js          # Styling config
├── 📄 postcss.config.js           # CSS processing
├── 📄 .eslintrc.cjs               # Linting rules
├── 📄 .gitignore                  # Git ignore
├── 📄 .env.example                # Env template
│
├── 📖 README.md                    # Project overview
├── 📖 QUICKSTART.md                # Quick setup guide
├── 📖 SETUP_GUIDE.md               # Detailed setup
├── 📖 API_SPECIFICATION.md         # API docs
├── 📖 CONVERSION_COMPLETE.md       # Summary
└── 📖 PROJECT_STRUCTURE.txt        # This file

---

## Component Flow

Login.jsx
    ↓ (successful login)
Layout.jsx
    ├── Sidebar.jsx (navigation)
    ├── Header.jsx (user info, theme toggle)
    └── <Route Content>
        ├── Dashboard.jsx (charts & stats)
        ├── EmployeeList.jsx → ProfileModal.jsx
        ├── AddEmployee.jsx (single + bulk)
        └── ManageUsers.jsx (admin only)

---

## State Management

AuthContext
    ├── user (username, role)
    ├── login()
    ├── logout()
    └── hasRole()

ThemeContext
    ├── isDarkMode
    └── toggleTheme()

---

## API Structure (services/api.js)

authAPI
    ├── login()
    └── verifyUser()

employeeAPI
    ├── getAll()
    ├── getById()
    ├── create()
    ├── update()
    ├── delete()
    └── bulkUpload()

dashboardAPI
    └── getStats()

userAPI
    ├── getAll()
    ├── create()
    ├── delete()
    └── updatePassword()

---

## Page → Streamlit Mapping

Login.jsx                 ← frontend/app.py (show_login)
Dashboard.jsx            ← frontend/views/dashboard.py
EmployeeList.jsx         ← frontend/views/employee_list.py
ProfileModal.jsx         ← frontend/views/profile_view.py
                          + frontend/views/edit_employee.py
AddEmployee.jsx          ← frontend/views/add_employee.py
ManageUsers.jsx          ← frontend/views/manage_users.py

---

## Key Features by File

📄 Login.jsx
    - Username/password form
    - Role-based redirect
    - Error handling

📄 Dashboard.jsx
    - 4 stat cards
    - 5 chart types (Recharts)
    - Recent hires table
    - Tab navigation

📄 EmployeeList.jsx
    - Search bar
    - Department filter
    - Status filter
    - Clickable table rows

📄 ProfileModal.jsx
    - 5 tabs (Personal, Work, Skills, Assets, Performance)
    - View/Edit modes
    - CV download
    - Photo display

📄 AddEmployee.jsx
    - Single entry form (all tables)
    - Bulk upload
    - Excel template download
    - Photo/CV upload

📄 ManageUsers.jsx
    - User list
    - Create user form
    - Delete user
    - Change password

---

## Styling Approach

✨ Tailwind CSS Classes
    ├── dark: prefix for dark mode
    ├── responsive breakpoints (sm:, md:, lg:)
    ├── hover: and focus: states
    └── custom colors in tailwind.config.js

🎨 Theme Colors
    ├── primary-* (blue shades)
    ├── gray-* (neutral colors)
    └── semantic colors (red, green, yellow)

---

## Installation Flow

1. cd react_conversion
2. npm install                 ← Install dependencies
3. npm run dev                 ← Start server
4. Browser → localhost:3000    ← View app

---

## Build Flow

1. npm run build              ← Create production build
2. dist/ folder created       ← Optimized files
3. npm run preview            ← Test production build
4. Deploy dist/ to server     ← Host anywhere

---

## File Sizes (Estimated)

src/
    components/ (~15 KB)
    pages/ (~30 KB)
    context/ (~5 KB)
    services/ (~8 KB)
    App.jsx (~2 KB)

Total Source: ~60 KB
Total with Dependencies: ~2 MB (node_modules)
Production Build: ~150 KB (gzipped)

---

## Tech Stack Overview

Core:
    React 18.3.1
    React Router 6.22.0
    Vite 5.1.4

UI:
    Tailwind CSS 3.4.1
    Lucide React (icons)
    Recharts 2.12.0 (charts)

Forms:
    React Hook Form 7.50.1
    XLSX 0.18.5 (Excel)

HTTP:
    Axios 1.6.7

Dev:
    ESLint
    PostCSS
    Autoprefixer

---

## Browser DevTools Tips

Console Tab:
    - See API errors
    - View mock data logs
    - Check component renders

Network Tab:
    - Monitor API calls (when connected)
    - Check request/response
    - View file uploads

Application Tab:
    - localStorage → Check 'theme' and 'user'
    - Session Storage
    - Cookies

---

## Environment Variables

Create .env file:
    VITE_API_BASE_URL=http://localhost:8000

Access in code:
    import.meta.env.VITE_API_BASE_URL

Note: Must start with VITE_ prefix!

---

## Common Commands

Development:
    npm run dev              # Start dev server
    npm run build            # Build for production
    npm run preview          # Preview production build
    npm run lint             # Check code quality

Package Management:
    npm install              # Install dependencies
    npm install <package>    # Add new package
    npm update              # Update packages
    npm audit fix           # Fix vulnerabilities

---

## Git Workflow

Ignored by .gitignore:
    node_modules/
    dist/
    .env
    .env.local

To version control:
    src/
    public/
    *.config.js
    *.md
    package.json

---

## Testing Checklist

□ npm install works
□ npm run dev starts server
□ Can login with admin/admin
□ Dashboard shows charts
□ Can search employees
□ Profile modal opens
□ Theme toggle works
□ Can navigate between pages
□ Protected routes work
□ Logout redirects to login

---

## Performance Tips

1. Lazy load routes
2. Optimize images
3. Enable gzip compression
4. Use CDN for assets
5. Implement caching
6. Code splitting
7. Tree shaking (automatic in Vite)

---

## Accessibility Features

✓ Semantic HTML
✓ ARIA labels
✓ Keyboard navigation
✓ Focus indicators
✓ Color contrast (dark/light)
✓ Screen reader friendly

---

## Mobile Responsiveness

Tailwind Breakpoints:
    sm: 640px
    md: 768px
    lg: 1024px
    xl: 1280px
    2xl: 1536px

Responsive Features:
    - Collapsible sidebar
    - Stacked form fields
    - Horizontal scroll tables
    - Touch-friendly buttons

---

## VS Code Extensions (Recommended)

1. ES7+ React/Redux/React-Native snippets
2. Tailwind CSS IntelliSense
3. ESLint
4. Prettier
5. Auto Import
6. Path Intellisense

---

Last Updated: December 22, 2025
